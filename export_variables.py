# export_variables.py
import config

# For the sake of this example, let's just print VERSION and PERIOD
print(f"export VERSION={config.VERSION}")
print(f"export PERIOD={config.PERIOD}")
# Add other variables as needed